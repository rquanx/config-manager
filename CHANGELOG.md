# Changelog

## 0.1.0

Initial MVP release.

- create config groups with a default snapshot
- add, delete, list, and switch group items
- store metadata and snapshots under `~/.config-manager`
- support managed files, directories, hidden files, and symlinks
- provide interactive confirmations with Ink and `--force` fallbacks
- include integration tests for core CLI flows
